import numpy as np
import random as rd

# RANDOM: giá trị = [0,1]

# seed: tạo element random giống nhau
np.random.seed(0)

# a_random = np.random.random(3)
# print(a_random)

# a_rand = np.random.rand(2,3)
# print(a_rand)

# normal:
# np.random.normal(mean,standard deviation,size)
# mean: gtri trung bình
# standard deviation: độ lệch chuẩn
# a_normal = np.random.normal(0,1,(2,3))
# print(a_normal)

# a_randint = np.random.randint(0,9,(2,3),int)
# print(a_randint)